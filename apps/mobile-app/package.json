{
	"name": "mobile-app",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "echo 'mobile-app dev placeholder'",
		"build": "mkdir -p dist && echo 'mobile-app build placeholder' > dist/.keep",
		"start": "echo 'mobile-app start placeholder'",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"test": "echo 'mobile-app test placeholder'",
		"test:run": "echo 'mobile-app test:run placeholder'"
	}
}
