services:
  # TanStack Start application with SQLite database
  openfit:
    image: openfit
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - openfit-data:/app/data
    env_file: ".env.local"
    networks:
      - app-net

volumes:
  openfit-data:

networks:
  app-net:
