# Open Fit Self-Hosted Configuration
# Run `bash setup.sh` to generate these values automatically

# Convex Backend Configuration (REQUIRED - generated by setup.sh)
INSTANCE_SECRET=
CONVEX_SELF_HOSTED_ADMIN_KEY=

# JWT Authentication Keys (REQUIRED - generated by setup.sh)
JWT_PRIVATE_KEY=
JWKS=

# Public Convex URL - what the browser uses to connect to Convex (REQUIRED)
# Change this if you're exposing via a reverse proxy or different hostname
NEXT_PUBLIC_CONVEX_URL=http://localhost:3210

# Optional: Change the app port (default: 3000)
# APP_PORT=3000
