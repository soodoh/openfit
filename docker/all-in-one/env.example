# OpenFit All-in-One Environment Configuration
# Copy this file to .env and fill in the values
#
# IMPORTANT: Do NOT use quotes around values in this file.
# Docker env_file format expects raw values without quotes.

# =============================================================================
# REQUIRED - Generate with: pnpm generate:keys
# =============================================================================

# Convex instance secret (32 hex bytes)
# Example: INSTANCE_SECRET=7d7794d965d3a998e90a28078f59eed14874a5d9f4f671ab5429c09d7987df4c
INSTANCE_SECRET=

# RSA private key for JWT signing (single line, spaces between segments)
# Example: JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY----- MIIEvQ... -----END PRIVATE KEY-----
JWT_PRIVATE_KEY=

# JSON Web Key Set for JWT verification
# Example: JWKS={"keys":[{"use":"sig","kty":"RSA",...}]}
JWKS=

# =============================================================================
# OPTIONAL - Admin key (auto-generated if not provided)
# =============================================================================

# Generate with:
# docker run --rm -e INSTANCE_SECRET='your-secret' \
#   --entrypoint ./generate_admin_key.sh \
#   ghcr.io/get-convex/convex-backend:latest
#
# CONVEX_SELF_HOSTED_ADMIN_KEY=

# =============================================================================
# OPTIONAL - URL Configuration (change for external access)
# =============================================================================

# Public URL for browser WebSocket connections
# Change to your server's IP/hostname if not running locally
# NEXT_PUBLIC_CONVEX_URL=http://localhost:3000/convex

# Auth callback URL (must match where users access the app)
# CONVEX_AUTH_SITE_URL=http://localhost:3000

# Host port to expose (docker-compose only)
# APP_PORT=3000

# =============================================================================
# OPTIONAL - Convex Configuration
# =============================================================================

# Instance name (for identification)
# INSTANCE_NAME=convex-self-hosted

# Document retention delay in seconds (default: 2 days)
# DOCUMENT_RETENTION_DELAY=172800
